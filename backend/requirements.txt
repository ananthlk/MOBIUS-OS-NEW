# Web framework
Flask==3.0.0

# Database (PostgreSQL) - updated for Python 3.13 compatibility
SQLAlchemy>=2.0.30
psycopg[binary]>=3.1.0
alembic>=1.13.0

# Google Cloud / Firestore
google-cloud-firestore==2.14.0

# Environment / config
python-dotenv==1.0.0

# Authentication (User Awareness Sprint)
bcrypt>=4.1.0
PyJWT>=2.8.0
pytz>=2024.1

# Google Sign-In (ID-token verification, sign-in only)
google-auth>=2.28.0

# HTTP client for calling internal skills (welcome email via mobius-skills/email)
requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-flask>=1.3.0
